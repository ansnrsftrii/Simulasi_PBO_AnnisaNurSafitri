<?php
// File: index.php
require_once 'koneksi/database.php';
require_once 'JalurPendaftaran.php';

// Inisialisasi Koneksi Database
$database = new Database();
$db = $database->getConnection();
?>
<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simulasi PBO - Panel Pendaftaran Mahasiswa Baru</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .card { margin-bottom: 30px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
        .table th { background-color: #e9ecef; }
    </style>
</head>
<body>

<div class="container my-5">
    <div class="text-center mb-5">
        <h1 class="fw-bold text-primary">Sistem Informasi Pendaftaran Mahasiswa Baru</h1>
        <p class="text-muted">Implementasi Abstraksi, Pewarisan, Enkapsulasi, dan Polimorfisme</p>
    </div>

    <div class="card">
        <div class="card-header bg-primary text-white fw-bold">
            📊 DAFTAR MAHASISWA BARU - JALUR REGULER
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered table-striped table-hover align-middle">
                    <thead>
                        <tr class="text-center">
                            <th width="5%">ID</th>
                            <th>Nama Calon</th>
                            <th>Asal Sekolah</th>
                            <th width="10%">Nilai Ujian</th>
                            <th>Atribut Unik Jalur (Polimorfik)</th>
                            <th>Biaya Dasar</th>
                            <th>Total Akhir (Overridden)</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php
                        $dataReguler = PendaftaranReguler::getDaftarReguler($db);
                        foreach ($dataReguler as $row) {
                            $objek = new PendaftaranReguler($row);
                            echo "<tr>";
                            echo "<td class='text-center'>" . $objek->getIdPendaftaran() . "</td>";
                            echo "<td>" . htmlspecialchars($objek->getNamaCalon()) . "</td>";
                            echo "<td>" . htmlspecialchars($objek->getAsalSekolah()) . "</td>";
                            echo "<td class='text-center'>" . $objek->getNilaiUjian() . "</td>";
                            echo "<td>"; 
                            $objek->tampilkanInfoJalur(); // Polimorfisme info jalur
                            echo "</td>";
                            echo "<td>Rp " . number_format($objek->getBiayaPendaftaranDasar(), 0, ',', '.') . "</td>";
                            echo "<td class='fw-bold text-success'>Rp " . number_format($objek->hitungTotalBiaya(), 0, ',', '.') . "</td>";
                            echo "</tr>";
                        }
                        ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-header bg-success text-white fw-bold">
            🏆 DAFTAR MAHASISWA BARU - JALUR PRESTASI (Diskon Rp50.000)
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered table-striped table-hover align-middle">
                    <thead>
                        <tr class="text-center">
                            <th width="5%">ID</th>
                            <th>Nama Calon</th>
                            <th>Asal Sekolah</th>
                            <th width="10%">Nilai Ujian</th>
                            <th>Atribut Unik Jalur (Polimorfik)</th>
                            <th>Biaya Dasar</th>
                            <th>Total Akhir (Overridden)</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php
                        $dataPrestasi = PendaftaranPrestasi::getDaftarPrestasi($db);
                        foreach ($dataPrestasi as $row) {
                            $objek = new PendaftaranPrestasi($row);
                            echo "<tr>";
                            echo "<td class='text-center'>" . $objek->getIdPendaftaran() . "</td>";
                            echo "<td>" . htmlspecialchars($objek->getNamaCalon()) . "</td>";
                            echo "<td>" . htmlspecialchars($objek->getAsalSekolah()) . "</td>";
                            echo "<td class='text-center'>" . $objek->getNilaiUjian() . "</td>";
                            echo "<td>"; 
                            $objek->tampilkanInfoJalur(); 
                            echo "</td>";
                            echo "<td>Rp " . number_format($objek->getBiayaPendaftaranDasar(), 0, ',', '.') . "</td>";
                            echo "<td class='fw-bold text-success'>Rp " . number_format($objek->hitungTotalBiaya(), 0, ',', '.') . "</td>";
                            echo "</tr>";
                        }
                        ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-header bg-warning text-dark fw-bold">
            💼 DAFTAR MAHASISWA BARU - JALUR KEDINASAN (Surcharge 25%)
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered table-striped table-hover align-middle">
                    <thead>
                        <tr class="text-center">
                            <th width="5%">ID</th>
                            <th>Nama Calon</th>
                            <th>Asal Sekolah</th>
                            <th width="10%">Nilai Ujian</th>
                            <th>Atribut Unik Jalur (Polimorfik)</th>
                            <th>Biaya Dasar</th>
                            <th>Total Akhir (Overridden)</th>
                        </tr>
                    </thead>
                    <tbody>
                        <?php
                        $dataKedinasan = PendaftaranKedinasan::getDaftarKedinasan($db);
                        foreach ($dataKedinasan as $row) {
                            $objek = new PendaftaranKedinasan($row);
                            echo "<tr>";
                            echo "<td class='text-center'>" . $objek->getIdPendaftaran() . "</td>";
                            echo "<td>" . htmlspecialchars($objek->getNamaCalon()) . "</td>";
                            echo "<td>" . htmlspecialchars($objek->getAsalSekolah()) . "</td>";
                            echo "<td class='text-center'>" . $objek->getNilaiUjian() . "</td>";
                            echo "<td>"; 
                            $objek->tampilkanInfoJalur(); 
                            echo "</td>";
                            echo "<td>Rp " . number_format($objek->getBiayaPendaftaranDasar(), 0, ',', '.') . "</td>";
                            echo "<td class='fw-bold text-danger'>Rp " . number_format($objek->hitungTotalBiaya(), 0, ',', '.') . "</td>";
                            echo "</tr>";
                        }
                        ?>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>